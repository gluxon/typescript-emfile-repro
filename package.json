{
  "name": "typescript-emfile-repro",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "clean": "rm -rf packages && rm tsconfig.json",
    "generate": "node generate.js",
    "repro": "tsc --watch --extendedDiagnostics",
    "repro-node": "node node-only-repro.mjs"
  },
  "dependencies": {
    "typescript": "^4.5.5"
  }
}
